[project]
name = "query_estimator"
version = "0.1.0"
license = "Apache-2.0"
description = "Query cost estimation for SQL Lab"
dependencies = [
    "apache-superset-core",
]

[project.optional-dependencies]
dev = []
