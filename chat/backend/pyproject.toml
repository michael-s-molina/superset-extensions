[project]
name = "chat"
version = "0.1.0"
license = "Apache-2.0"
dependencies = [
    "apache-superset-core",
    "mcp",
]

[tool.apache_superset_extensions.build]
include = [
    "src/michael_s_molina/chat/**/*.py",
]
exclude = []
